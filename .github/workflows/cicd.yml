
# Nmae of workflow
name: CICD

#manual click delivver workflow when user needs
on:
 workflow_dispatch: 
jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code  
        run: echo " code clone "
  build:
    needs: [code]
    runs-on: ubuntu-latest
    steps:
      - name: build docker container  
        run: echo " Docker build"

  test:
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - name: test container 
        run: echo "test container"

  deploy:
    needs: [build,test]
    runs-on: ubuntu-latest
    steps:
        - name: deploy app
          run: echo " docker compose deply"
        
