# This is work flow name
 name: Hello workflow

# Runs on trigger 
 on:
   push:
     branches: [main]

# jobs inside workflow
 jobs:

# NAME OF WORKFLOW job

greet:
    # This is GitHub runner each job runs on it
    runs-on: ubuntu-latest

    steps:
      - name: Say hello to users
        run: echo "hello all"

      - name: Say hello to username
        run: echo "Hello $USER"

 details:
    runs-on: ubuntu-latest

    steps:
      - name: Version of python
        run: python --version
